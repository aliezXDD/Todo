### 阶段一：项目骨架 + 数据层

```
请根据附件「待办软件.md」构建 Todo 项目的第一阶段：项目骨架和完整数据层。

具体要求：
1. 生成 project 级和 app 级 build.gradle.kts，包含文档第六节的所有依赖
2. 生成 AndroidManifest.xml（竖屏锁定、HiltWorkerFactory配置）
3. 生成 TodoApp.kt（@HiltAndroidApp，暂不注册WorkManager）
4. 生成 MainActivity.kt（@AndroidEntryPoint，暂时只显示一个空的 Compose Text("Todo")）
5. 完整生成 data/ 目录下所有文件：
   - entity/：TodoEntity、PresetEntity、RecycleBinEntity、DailyStatsEntity（严格按文档第八节）
   - dao/：TodoDao、PresetDao、RecycleBinDao、DailyStatsDao（严格按文档第七节）
   - local/AppDatabase.kt（4张表，version=1）
   - local/DatabaseModule.kt（Hilt Module，按文档第十七节）
   - datastore/ThemePreferences.kt（DataStore保存theme_mode，默认"system"）
6. 完整生成 data/repository/ 下4个Repository类，封装对应DAO操作
7. 生成 util/DateUtils.kt（按文档第十五节）和 util/Constants.kt
8. 包名 com.todo，所有中文字符串按文档规范

请确保此阶段代码可以编译通过（UI暂时只需空页面）。
```

**✅ 检查方法：**
- 在 Android Studio 中 `Build > Rebuild Project`，编译无报错
- 检查 `app/src/main/java/com/todo/data/` 下文件齐全（4 entity + 4 dao + database + module + datastore + 4 repository）
- 检查 `AppDatabase` 包含4个抽象 DAO 方法
- 检查 `ThemePreferences` 有读写 theme_mode 的方法
- 运行 App 能看到空白页面显示 "Todo" 文字即可

---

### 阶段二：领域层 + 主题 + 通用组件

```
在阶段一基础上继续构建第二阶段：领域层和UI基础组件。

具体要求：
1. 生成 domain/model/ 下所有模型类：
   - Todo.kt、Preset.kt、DailyRecord.kt、DailyStats.kt、RecycleBinItem.kt
   - 按文档标注 @Stable 或 @Immutable
2. 生成 domain/usecase/ 下所有用例类：
   - AddTodoUseCase、UpdateTodoUseCase、DeleteTodoUseCase、ReorderTodosUseCase
   - ToggleTodoUseCase、GetTodayTodosUseCase、GetHistoryRecordsUseCase
   - PresetUseCases、ArchiveUseCase、CleanupUseCase、GetStatsUseCase
   - 每个UseCase通过Hilt @Inject constructor注入Repository
   - ArchiveUseCase 和 CleanupUseCase 按文档第九节业务逻辑实现
3. 生成 ui/theme/ 下所有文件：
   - Color.kt（文档5.6节色彩系统）
   - Type.kt（文档5.7节字体排版）
   - Shape.kt（圆角定义）
   - Theme.kt（TodoTheme，支持浅色/深色/跟随系统切换，读取ThemePreferences）
4. 生成 ui/component/ 下所有通用组件：
   - GlassCard.kt（文档5.2节参数）
   - GlassButton.kt
   - GlassFAB.kt（56dp圆形，"+"号）
   - GlassBottomSheet.kt（文档5.3节参数）
   - GlassTopBar.kt
   - GlassDialog.kt（文档5.4节参数）
   - GradientBackground.kt（文档5.1节渐变色）
   - EmptyState.kt（居中显示图标+文字）
   - UndoSnackbar.kt（"已删除"+"撤销"按钮，3秒自动消失）
5. 更新 MainActivity.kt，用 TodoTheme + GradientBackground 包裹内容，
   暂时内容显示一个 GlassCard 内含 Text("主题测试") 用于验证主题效果

所有组件中的中文文字严格按文档第十四节。
```

**✅ 检查方法：**
- 编译通过无报错
- 运行 App，能看到渐变背景（浅色：淡薰衣草→淡粉紫）
- 能看到一张半透明毛玻璃卡片显示 "主题测试"
- 在系统设置中切换深色模式，重新打开 App 背景变为深色（深靛蓝→深海蓝）
- 检查 `domain/usecase/` 下有 11 个 UseCase 文件
- 检查 `ui/component/` 下有 9 个组件文件

---

### 阶段三：导航框架 + 待办页面（核心功能）

```
在阶段二基础上继续构建第三阶段：导航和待办页面全部功能。

具体要求：
1. 生成 ui/navigation/ 下所有文件：
   - Screen.kt（密封类，5个路由，按文档第十六节）
   - BottomNavBar.kt（3 Tab毛玻璃底栏，文档5.5节样式，标签"预设""待办""设置"）
   - NavGraph.kt（导航图，默认首页 Screen.Todo，Tab切换 + push页面规则按文档第十六节）

2. 更新 MainActivity.kt：
   - 用 NavGraph + BottomNavBar 替换临时内容
   - 预设页和设置页暂用占位 Text

3. 生成 ui/screen/todo/ 下所有文件：
   - TodoViewModel.kt（UiState按文档11.1节，注入所有相关UseCase）
   - TodoScreen.kt：
     a. MiniStatsCard（圆形进度环 + "{N}/{M} 已完成"，点击导航到ChartScreen）
     b. TodaySection（标题"今日"+中文日期，LazyColumn展示TodoItemRow）
     c. HistorySection（近7天HistoryDayCard列表）
     d. GlassFAB 右下角
   - component/MiniStatsCard.kt
   - component/TodaySection.kt
   - component/TodoItemRow.kt（圆角方形勾选框+文字，完成态加删除线+透明度0.5）
   - component/HistorySection.kt
   - component/HistoryDayCard.kt（不可编辑，显示日期+完成率）
   - component/AddTodoSheet.kt（两个Tab："手动输入"和"从预设选择"，按文档4.3节全部交互）
   - component/EditTodoSheet.kt（编辑+删除，按文档4.4节）

4. 实现以下交互：
   - 点击FAB → 弹出AddTodoSheet
   - 手动输入添加待办 → 添加到今日列表末尾，Sheet不关闭
   - 从预设选择 → 单击直接添加，长按进入多选+"添加选中项"
   - 勾选/取消勾选 → 数据库更新 + 动画（勾选框弹性+删除线划出300ms）
   - 长按待办(0.3秒) → 弹出EditTodoSheet
   - 拖拽排序 → 使用 sh.calvin.reorderable，松手保存sortOrder，拖拽时放大1.05+触觉反馈
   - 删除待办 → 确认对话框 → 移入回收站 + UndoSnackbar 3秒可撤销
   - 空状态显示 "点击右下角 + 添加第一条待办"

5. 所有动画按文档第十二节规范实现

所有中文文字严格按文档第十四节。
```

**✅ 检查方法：**
- 编译运行，底部显示3个Tab（预设/待办/设置），默认选中"待办"
- Tab切换正常，预设和设置页显示占位文字
- 待办页顶部有迷你统计卡（显示 "0/0 已完成"）
- 点击右下角 FAB → 弹出毛玻璃底部Sheet，有两个Tab
- 手动输入一条待办 → 出现在今日列表中（带滑入动画）
- 点击勾选框 → 文字加删除线+变透明（带动画）
- 长按某条 → 弹出编辑Sheet，可修改内容和删除
- 长按拖动 → 可排序，被拖项放大+触觉反馈
- 删除后出现 "已删除" + "撤销" 提示

---

### 阶段四：预设页面 + 设置页面

```
在阶段三基础上继续构建第四阶段：预设管理和设置页面。

具体要求：
1. 生成 ui/screen/preset/ 下所有文件：
   - PresetViewModel.kt（UiState按文档11.2节）
   - PresetScreen.kt
   - component/PresetTopBar.kt（标题"我的预设"，右侧"+"按钮，多选模式显示"已选N项"+"全选"+"删除"）
   - component/PresetItemRow.kt（毛玻璃卡片样式）
   - component/PresetEditDialog.kt（新建模式标题"新建预设"/编辑模式标题"编辑预设"，按文档4.5节）

2. 预设页交互：
   - 点击"+"→ 弹出 PresetEditDialog（新建模式）
   - 单击某条 → 弹出编辑模式对话框
   - 长按 → 进入多选模式，该条自动选中
   - 多选模式下单击切换选中
   - "全选"按钮、"删除"按钮（带确认对话框"确定删除选中的N条预设吗？"）
   - 按返回键退出多选
   - 空状态："点击右上角 + 创建常用待办预设"

3. 生成 ui/screen/settings/ 下所有文件：
   - SettingsViewModel.kt（读写ThemePreferences）
   - SettingsScreen.kt：
     a. 毛玻璃顶栏标题"设置"
     b. "外观主题"设置项：三选一单选组（"跟随系统"/"浅色模式"/"深色模式"）
     c. "回收站"设置项：可点击行+右箭头，点击导航到RecycleBinScreen
     d. "关于"设置项：显示"Todo v1.0"

4. 主题切换：
   - 选择主题后立即保存DataStore并全局生效
   - 切换时无需重启App

5. 确保待办页AddTodoSheet的"从预设选择"Tab能正确读取预设列表、搜索、单选添加、多选添加

所有中文文字严格按文档第十四节。
```

**✅ 检查方法：**
- 切换到"预设"Tab，显示空状态 "点击右上角 + 创建常用待办预设"
- 点击"+" → 弹出对话框标题 "新建预设"，输入内容点"创建" → 列表出现该预设
- 单击预设 → 弹出 "编辑预设" 对话框，可修改保存
- 长按预设 → 进入多选模式，顶栏变为 "已选 1 项" + "全选" + "删除"
- 批量删除功能正常
- 回到待办页，FAB → "从预设选择" Tab → 能看到刚创建的预设
- 切换到"设置"Tab → 显示3个设置项
- 切换主题为"深色模式" → 整个App立即变为深色渐变背景+深色毛玻璃卡片
- 切回"跟随系统" → 恢复跟随系统设置

---

### 阶段五：回收站 + 统计图表

```
在阶段四基础上继续构建第五阶段：回收站和统计图表页面。

具体要求：
1. 生成 ui/screen/recyclebin/ 下所有文件：
   - RecycleBinViewModel.kt（UiState按文档11.4节）
   - RecycleBinScreen.kt：
     a. push页面，顶部返回箭头+标题"回收站"
     b. 按deletedAt倒序，粘性标题按日期分组（格式"2025年1月15日"）
     c. 每条显示：内容+原始日期+完成状态标记
     d. 长按进入多选（与预设页相同逻辑）
     e. "已选N项"+"全选"+"永久删除"
     f. 确认对话框"确定永久删除N条记录？此操作不可撤销"
     g. 空状态"回收站为空"
     h. 无恢复功能

2. 生成 ui/screen/chart/ 下所有文件：
   - ChartViewModel.kt（UiState按文档11.3节，注入GetStatsUseCase）：
     a. 加载近30天DailyStatsEntity数据
     b. 计算 averageRate（近30天平均完成率）
     c. 计算 maxRate（单日最高完成率）
     d. 计算 totalCompleted（累计completedCount总和）
     e. 计算 consecutiveDays（从今日往前连续完成率100%的天数）
     f. chartType 支持 BAR 和 LINE 切换

   - ChartScreen.kt：
     a. push页面，顶部返回箭头+标题"完成统计"
     b. 右上角切换按钮，在"柱状图"和"折线图"之间切换
     c. 使用 Vico compose-m3 绘制图表：
        - X轴=日期（每隔5天显示一个标签）
        - Y轴=完成率 0%~100%
        - 柱状图：每根柱子颜色根据完成率渐变（0%=红色 #f8756c，50%=橙色 #f7c476，100%=绿色 #96c797）此处的颜色若与文档不同，以此处为准
        - 折线图：线条颜色为主题强调色，数据点带圆点标记
     d. 图表下方4个统计卡片（毛玻璃样式），横排2x2网格：
        - "平均完成率" → 显示百分比如 "75%"
        - "最高完成率" → 显示百分比如 "100%"
        - "累计完成" → 显示数字如 "42"
        - "连续全部完成" → 显示 "{N} 天"
     e. 无数据时图表区域显示空状态提示

3. 确保从待办页 MiniStatsCard 点击能正确 push 到 ChartScreen
4. 确保从设置页 "回收站" 行点击能正确 push 到 RecycleBinScreen
5. 确保待办页删除待办后，回收站中能看到该条记录

所有中文文字严格按文档第十四节。
```

**✅ 检查方法：**
- 在待办页添加几条待办，删除其中一条
- 进入设置 → 点击"回收站" → 页面push进入，能看到刚删除的待办
- 回收站条目显示内容+原始日期+完成状态
- 长按进入多选 → 顶栏显示 "已选 1 项" → 点"永久删除" → 确认后消失
- 返回箭头正常返回设置页
- 在待办页勾选几条待办，点击顶部迷你统计卡
- 页面push进入"完成统计"页面
- 能看到图表（至少今天有一根柱子/一个数据点）
- 右上角点击切换 → 柱状图/折线图切换正常
- 下方4个统计卡片显示中文标签和对应数值
- 返回箭头正常返回待办页

---

### 阶段六：WorkManager归档 + 动画打磨 + 最终检查

```
在阶段五基础上完成最后阶段：WorkManager每日归档、动画完善和全局检查。

具体要求：
1. 生成 worker/DailyArchiveWorker.kt：
   - @HiltWorker 注解
   - 注入 ArchiveUseCase 和 CleanupUseCase
   - doWork() 中依次执行归档和清理，按文档第十节实现
   - 异常时返回 Result.retry()

2. 更新 TodoApp.kt：
   - onCreate 中注册 WorkManager 每日定时任务
   - 使用 PeriodicWorkRequestBuilder 1天周期
   - 初始延迟 = DateUtils.calculateDelayUntilMidnight()
   - ExistingPeriodicWorkPolicy.KEEP 避免重复注册
   - 配置 HiltWorkerFactory（禁用默认 WorkManager 初始化）

3. App启动时归档检查：
   - TodoApp.onCreate 或 MainActivity 中检查 DataStore 的 last_archive_date
   - 如果不是今天，立即执行一次 ArchiveUseCase + CleanupUseCase
   - 完成后更新 last_archive_date = 今天

4. 确保归档流程正确（文档9.1节）：
   - date < 今天 且未写入DailyStats的日期 → 写入统计
   - date < (今天-7天) 的TodoEntity → 转为RecycleBinEntity → 删除原始
   - deletedAt < (今天-30天) 的回收站记录 → 永久删除

5. 实时统计更新（文档9.2节）：
   - 确保每次 toggle 待办完成状态时，同步刷新今日 DailyStatsEntity
   - 确保添加/删除待办时也刷新今日统计

6. 动画最终打磨，逐项检查文档第十二节：
   - 待办勾选：勾选框 scale 弹性 0→1，文字 alpha 1→0.5 + 删除线从左到右 300ms
   - 添加待办：新条目从底部 slideIn + fadeIn 250ms
   - 删除待办：向左 slideOut + fadeOut 200ms + 下方上移 200ms
   - 拖拽中：scale=1.05 + elevation=16dp + 其他项让位动画
   - 底部Sheet：slideUp + fadeIn 300ms EaseOutCubic
   - Tab切换：crossfade 200ms
   - FAB按下：scale 0.9→1.0 弹性回弹
   - 多选勾选：圆形框 scale 弹性 + 颜色渐变 200ms

7. 性能优化检查（文档第十三节）：
   - LazyColumn 使用 key = { it.id }
   - Room Flow 使用 distinctUntilChanged()
   - 背景 Brush 使用 remember 缓存
   - 数据类标注 @Stable / @Immutable

8. 全局最终检查：
   - 所有界面文字为简体中文（逐项比对文档第十四节表格）
   - 日期格式均为中文（"2025年1月15日 周三"）
   - 深色/浅色主题切换所有页面表现正确
   - 底部导航栏毛玻璃效果+选中态颜色正确
   - 所有 GlassCard/GlassDialog/GlassBottomSheet 参数符合文档第五节
   - 回收站和图表页 push/pop 导航正常
   - 编译无警告无报错

所有中文文字严格按文档（待办软件.md）第十四节。
```

**✅ 检查方法：**

**归档功能验证：**
- 添加几条今日待办并勾选部分完成
- 手动将手机系统日期改为明天
- 重新打开App → 昨日待办应出现在"往日记录"区域（HistoryDayCard）
- 迷你统计卡重置为 "0/0 已完成"（新的一天）
- 将日期改为8天后 → 重新打开 → 最初那天的记录应从往日记录消失（已移入回收站）
- 进入回收站 → 能看到被归档移入的记录
- 将日期改为38天后 → 重新打开 → 回收站中那些记录应自动永久删除

**实时统计验证：**
- 添加3条待办，勾选2条 → 迷你统计卡显示 "2/3 已完成"
- 进入图表页 → 今天的柱子/数据点对应约 67% 完成率
- 下方统计卡片数值正确

**动画验证：**
- 勾选待办 → 观察勾选框弹性缩放 + 删除线从左到右划出
- 添加待办 → 新条目从下方滑入
- 删除待办 → 条目向左滑出消失
- 拖拽排序 → 被拖项明显放大 + 手机有振动反馈
- FAB 按下松开 → 有缩放弹性效果

**全局外观验证：**
- 浅色模式：渐变背景淡薰衣草→淡粉紫，卡片半透明白色
- 深色模式：渐变背景深靛蓝→深海蓝，卡片微透明
- 底部导航栏半透明毛玻璃，选中Tab为紫色强调色
- 所有对话框、底部Sheet、卡片圆角和边框符合文档规范

**最终编译检查：**
- `Build > Clean Project` → `Build > Rebuild Project` → 0 errors 0 warnings
- 运行在 API 31+ 设备/模拟器上无崩溃

---

## 阶段总览

| 阶段 | 内容 | 预期文件数 | 核心验证点 |
|------|------|-----------|-----------|
| 一 | 项目骨架+数据层 | ~20 | 编译通过，空页面显示 |
| 二 | 领域层+主题+通用组件 | ~25 | 渐变背景+毛玻璃卡片可见，主题切换生效 |
| 三 | 导航+待办页全功能 | ~15 | 增删改查拖拽排序全部可用 |
| 四 | 预设页+设置页 | ~10 | 预设CRUD+多选删除+主题切换即时生效 |
| 五 | 回收站+统计图表 | ~6 | 回收站查看删除+图表展示切换 |
| 六 | WorkManager+动画+收尾 | ~3 | 归档自动执行+全部动画到位+零报错 |