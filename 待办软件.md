# Todo 完整项目规范文档

> 本文档为 Codex AI 的唯一参考源，包含项目的全部设计细节、技术规范、数据结构、页面逻辑和实现步骤。请严格按照此文档在 `D:\Code\2.Projects\Todo` 目录下构建项目。所有用户可见的界面文字必须使用**简体中文**。

---

## 一、项目概览

| 属性 | 值 |
|------|-----|
| 项目名 | Todo |
| 项目根目录 | `D:\Code\2.Projects\Todo` |
| 包名 | com.todo |
| 语言 | Kotlin |
| 最低 SDK | API 31 (Android 12) |
| 目标 SDK | API 34 (Android 14) |
| UI 框架 | Jetpack Compose + Material3 |
| 架构 | MVVM + Clean Architecture |
| 导航 | Navigation Compose（底部 3 Tab + push 页面） |
| 本地存储 | Room（4 张表）+ DataStore Preferences |
| 依赖注入 | Hilt |
| 图表 | Vico 2.0.x (compose-m3) |
| 拖拽排序 | sh.calvin.reorderable 1.5.x |
| 定时任务 | WorkManager |
| 设计风格 | 毛玻璃拟态（Glassmorphism）|
| 屏幕方向 | 仅竖屏（portrait locked）|
| 界面语言 | 简体中文（所有用户可见文字均为中文） |

---

## 二、完整目录结构

```
D:\Code\2.Projects\Todo\
└── app/src/main/java/com/todo/
    ├── TodoApp.kt                               // @HiltAndroidApp
    ├── MainActivity.kt                          // @AndroidEntryPoint, 锁定竖屏
    │
    ├── data/
    │   ├── local/
    │   │   ├── AppDatabase.kt                   // Room Database, 4 张表
    │   │   ├── DatabaseModule.kt                // Hilt Module 提供 DB + DAO
    │   │   ├── dao/
    │   │   │   ├── TodoDao.kt
    │   │   │   ├── PresetDao.kt
    │   │   │   ├── RecycleBinDao.kt
    │   │   │   └── DailyStatsDao.kt
    │   │   ├── entity/
    │   │   │   ├── TodoEntity.kt
    │   │   │   ├── PresetEntity.kt
    │   │   │   ├── RecycleBinEntity.kt
    │   │   │   └── DailyStatsEntity.kt
    │   │   └── datastore/
    │   │       └── ThemePreferences.kt          // DataStore 保存主题选项
    │   │
    │   └── repository/
    │       ├── TodoRepository.kt
    │       ├── PresetRepository.kt
    │       ├── RecycleBinRepository.kt
    │       └── StatsRepository.kt
    │
    ├── domain/
    │   ├── model/
    │   │   ├── Todo.kt                          // UI 层使用的领域模型
    │   │   ├── Preset.kt
    │   │   ├── DailyRecord.kt                  // 某天的待办列表打包(date + List<Todo>)
    │   │   ├── DailyStats.kt                   // date + totalCount + completedCount
    │   │   └── RecycleBinItem.kt
    │   │
    │   └── usecase/
    │       ├── AddTodoUseCase.kt
    │       ├── UpdateTodoUseCase.kt
    │       ├── DeleteTodoUseCase.kt
    │       ├── ReorderTodosUseCase.kt
    │       ├── ToggleTodoUseCase.kt
    │       ├── GetTodayTodosUseCase.kt
    │       ├── GetHistoryRecordsUseCase.kt
    │       ├── PresetUseCases.kt                // 增删改查预设
    │       ├── ArchiveUseCase.kt                // 每日归档：今日→往日+写入统计
    │       ├── CleanupUseCase.kt                // 7天→回收站，30天永删
    │       └── GetStatsUseCase.kt               // 查询统计数据
    │
    ├── ui/
    │   ├── theme/
    │   │   ├── Color.kt                         // 浅色/深色色值定义
    │   │   ├── Type.kt                          // 字体排版
    │   │   ├── Shape.kt                         // 圆角定义
    │   │   └── Theme.kt                         // TodoTheme 切换深浅色
    │   │
    │   ├── component/                           // 全局通用组件
    │   │   ├── GlassCard.kt                    // 毛玻璃卡片
    │   │   ├── GlassButton.kt                  // 毛玻璃按钮
    │   │   ├── GlassFAB.kt                     // 右下角浮动按钮
    │   │   ├── GlassBottomSheet.kt             // 毛玻璃底部弹出面板
    │   │   ├── GlassTopBar.kt                  // 毛玻璃顶栏
    │   │   ├── GlassDialog.kt                  // 毛玻璃对话框
    │   │   ├── GradientBackground.kt           // 全屏渐变背景
    │   │   ├── EmptyState.kt                   // 空状态占位组件
    │   │   └── UndoSnackbar.kt                 // 删除撤销提示
    │   │
    │   ├── navigation/
    │   │   ├── Screen.kt                        // 路由密封类
    │   │   ├── BottomNavBar.kt                  // 底部导航栏（3 Tab）
    │   │   └── NavGraph.kt                      // 导航图
    │   │
    │   └── screen/
    │       ├── todo/
    │       │   ├── TodoScreen.kt                // 待办主页面
    │       │   ├── TodoViewModel.kt
    │       │   └── component/
    │       │       ├── MiniStatsCard.kt         // 顶部迷你统计卡（点击进入图表）
    │       │       ├── TodaySection.kt          // "今日"卡片区域
    │       │       ├── TodoItemRow.kt           // 单条待办行（勾选框+文字）
    │       │       ├── HistorySection.kt        // 往日记录列表
    │       │       ├── HistoryDayCard.kt        // 单天往日卡片（不可编辑）
    │       │       ├── AddTodoSheet.kt          // 添加待办底部Sheet
    │       │       └── EditTodoSheet.kt         // 编辑待办底部Sheet
    │       │
    │       ├── preset/
    │       │   ├── PresetScreen.kt              // 预设管理页
    │       │   ├── PresetViewModel.kt
    │       │   └── component/
    │       │       ├── PresetTopBar.kt          // 新建/选择/删除按钮栏
    │       │       ├── PresetItemRow.kt         // 单条预设行
    │       │       └── PresetEditDialog.kt      // 新建/编辑预设弹窗
    │       │
    │       ├── settings/
    │       │   ├── SettingsScreen.kt            // 设置页
    │       │   └── SettingsViewModel.kt
    │       │
    │       ├── recyclebin/
    │       │   ├── RecycleBinScreen.kt          // 回收站页面（push进入）
    │       │   └── RecycleBinViewModel.kt
    │       │
    │       └── chart/
    │           ├── ChartScreen.kt              // 统计图表全屏页（push进入）
    │           └── ChartViewModel.kt
    │
    ├── worker/
    │   └── DailyArchiveWorker.kt               // WorkManager 每日零点归档
    │
    └── util/
        ├── DateUtils.kt                         // 日期工具函数
        └── Constants.kt                         // 常量
```

---

## 三、数据库设计

### 3.1 TodoEntity

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| content | String | 待办内容文字 |
| isCompleted | Boolean | 是否完成，默认 false |
| date | String | 格式 "yyyy-MM-dd"，标记属于哪一天 |
| sortOrder | Int | 排序序号，值越小越靠前 |
| createdAt | Long | 创建时间戳 |

### 3.2 PresetEntity

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| content | String | 预设内容 |
| createdAt | Long | 创建时间戳 |

### 3.3 RecycleBinEntity

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| originalDate | String | 原始所属日期 "yyyy-MM-dd" |
| content | String | 待办内容 |
| wasCompleted | Boolean | 被删除时的完成状态 |
| deletedAt | Long | 移入回收站的时间戳 |

### 3.4 DailyStatsEntity

| 字段 | 类型 | 说明 |
|------|------|------|
| date | String | 主键，"yyyy-MM-dd" |
| totalCount | Int | 当日总待办数 |
| completedCount | Int | 当日已完成数 |

### 3.5 数据生命周期流转

```
用户添加 → TodoEntity(date=今天)
                │
    ┌───────────┤ 每日零点归档(ArchiveUseCase)
    │           ▼
    │   TodoEntity(date=过去某天) ─→ 同时写入 DailyStatsEntity
    │           │
    │           │ 超过7天(CleanupUseCase)
    │           ▼
    │   RecycleBinEntity
    │           │
    │           │ 超过30天
    │           ▼
    │       永久删除
    │
    └── DailyStatsEntity 永久保留（供图表使用）
```

---

## 四、页面与交互细节

> **重要：以下所有引号内的中文文字即为最终界面显示文字，请原样使用，不得替换为英文或其他语言。**

### 4.1 底部导航栏（3 Tab）

| 位置 | 标签 | 图标 | 对应页面 |
|------|------|------|----------|
| 左 | 预设 | 文档图标 | PresetScreen |
| 中 | 待办 | 勾选图标 | TodoScreen（默认首页）|
| 右 | 设置 | 齿轮图标 | SettingsScreen |

底部栏本身也是毛玻璃效果，浅色模式 `White alpha=0.6`，深色模式 `White alpha=0.1`，高度 64dp，圆角顶部 20dp。

---

### 4.2 待办页面（TodoScreen）

**布局从上到下：**

1. **MiniStatsCard**（迷你统计卡片）
   - 显示今日完成进度：圆形进度环 + "3/5 已完成" 文字
   - 点击 → push 进入 ChartScreen 全屏统计
   - 毛玻璃卡片样式，高度约 80dp

2. **TodaySection**（今日待办卡片）
   - 标题："今日" + 日期文字（如 "2025年1月15日 周三"）
   - 内容：LazyColumn 展示今日所有 TodoItemRow
   - 每条 TodoItemRow：左侧圆角方形勾选框 + 内容文字
   - 已完成的条目文字加删除线，透明度降低至 0.5
   - 高度自适应内容，最大高度不超过屏幕 60%（超出可内部滚动）
   - 空状态：显示 "点击右下角 + 添加第一条待办"

3. **HistorySection**（往日记录）
   - 按日期从近到远排列，每天一个 HistoryDayCard
   - HistoryDayCard 样式与 TodaySection 相似但不可编辑、不可拖拽
   - 只显示近 7 天的历史记录
   - 每张卡片标题显示日期 + 完成率（如 "4/6 完成"）

4. **GlassFAB**（右下角浮动按钮）
   - 圆形，直径 56dp，内部 "+" 号
   - 点击弹出 AddTodoSheet

**交互逻辑：**

- **勾选/取消勾选：** 点击勾选框 → 立即更新数据库 isCompleted 字段 → UI 响应式刷新
- **长按某条今日待办（0.3秒触发）：** 弹出 EditTodoSheet（含编辑输入框 + 删除按钮）
- **长按并拖动：** 进入拖拽排序模式，松手后保存新的 sortOrder
- **拖拽时：** 被拖拽项放大 1.05 倍 + 阴影加深 + 触觉反馈(HapticFeedback)

---

### 4.3 添加待办（AddTodoSheet）

弹出底部Sheet，毛玻璃样式，内含两个 Tab：

**Tab 1："手动输入"**
- 一个文本输入框，placeholder "输入待办内容..."
- 右侧"添加"按钮
- 输入非空时按钮可用，点击后添加到今日待办列表末尾，Sheet 不关闭（方便连续添加）
- 输入框下方显示"完成"按钮，点击关闭 Sheet

**Tab 2："从预设选择"**
- 显示所有预设的列表，每条可点击
- 点击某条预设 → 立即添加到今日待办列表末尾
- 支持多选：长按进入多选 → 底部出现"添加选中项"按钮
- 顶部有搜索框，placeholder "搜索预设..."，可按关键字筛选预设
- 若无预设，显示引导文字 "暂无预设，去预设页面添加吧"

---

### 4.4 编辑待办（EditTodoSheet）

长按今日某条待办弹出，毛玻璃底部 Sheet：

- 顶部标题 "编辑待办"
- 文本输入框，预填充当前内容，自动获取焦点并光标移至末尾
- 底部两个按钮横排：
  - "保存" — 更新内容到数据库，关闭 Sheet
  - "删除" — 弹出确认对话框（GlassDialog），确认后移入回收站 + 显示 UndoSnackbar（3 秒内可撤销）

---

### 4.5 预设页面（PresetScreen）

**布局：**

1. **PresetTopBar**（毛玻璃顶栏）
   - 标题 "我的预设"
   - 右侧按钮：普通模式显示 "+" 新建按钮；多选模式显示 "删除(N)" + "全选" 按钮

2. **预设列表**
   - LazyColumn 展示所有预设，每条 PresetItemRow
   - PresetItemRow：毛玻璃卡片，显示内容文字
   - 普通模式：
     - 单击某条 → 弹出 PresetEditDialog（编辑模式，可修改内容）
     - 长按某条 → 进入多选模式，该条自动选中
   - 多选模式：
     - 每条左侧显示圆形勾选框
     - 单击切换选中/取消
     - 顶栏变为 "已选 N 项" + "全选" + "删除"
     - 点击"删除" → 弹出确认对话框 "确定删除选中的 N 条预设吗？" → 确认后批量删除
     - 按返回键或点击空白区域 → 退出多选模式
   - 空状态：显示 "点击右上角 + 创建常用待办预设"

3. **PresetEditDialog**（毛玻璃对话框）
   - 新建模式：标题 "新建预设"，空输入框，placeholder "输入预设内容..."，"取消" / "创建" 按钮
   - 编辑模式：标题 "编辑预设"，预填内容，"取消" / "保存" 按钮

---

### 4.6 设置页面（SettingsScreen）

**布局：**

1. **毛玻璃顶栏** — 标题 "设置"

2. **设置项列表** — 每项都是毛玻璃卡片样式：

   | 设置项 | 控件 | 说明 |
   |--------|------|------|
   | 外观主题 | 三选一单选组 | "跟随系统" / "浅色模式" / "深色模式"，默认 "跟随系统" |
   | 回收站 | 可点击行 + 右箭头 | 点击 push 进入 RecycleBinScreen |
   | 关于 | 文字显示 | 版本号 + "Todo v1.0" |

**主题切换机制：**
- 选项保存到 DataStore Preferences，key = "theme_mode"，值 = "system" / "light" / "dark"
- App 启动时读取该值并应用
- 切换时全局即时响应，无需重启

---

### 4.7 回收站页面（RecycleBinScreen）

- push 方式从设置页进入，顶部有返回箭头
- 顶栏标题 "回收站"
- 列表按 `deletedAt` 倒序排列
- 用粘性标题（Sticky Header）按日期分组，格式 "2025年1月15日"
- 每条显示：内容 + 原始日期 + 完成状态标记
- 支持多选模式（与预设页相同交互逻辑）：
  - 长按进入多选
  - 顶栏显示 "已选 N 项" + "全选" + "永久删除"
  - 点击 "永久删除" → 弹出确认对话框 "确定永久删除 N 条记录？此操作不可撤销"
- 空状态：显示 "回收站为空"
- **无恢复功能** — 回收站只能查看和永久删除

---

### 4.8 统计图表页面（ChartScreen）

- push 方式从待办页 MiniStatsCard 点击进入
- 顶部返回箭头 + 标题 "完成统计"

**图表内容：**

- 默认展示近 30 天数据
- X 轴 = 日期（每隔 5 天显示一个标签）
- Y 轴 = 完成率 0% ~ 100%
- 数据来源：DailyStatsEntity 表
- 图表类型切换：右上角切换按钮，可在"柱状图"和"折线图"之间切换
- 柱状图：每根柱子颜色根据完成率渐变（0%=红色，50%=橙色，100%=绿色）
- 折线图：线条颜色为主题色，数据点带圆点标记

**图表下方统计卡片（简体中文标签）：**

| 统计项 | 显示标签 | 计算方式 |
|--------|----------|----------|
| 平均完成率 | "平均完成率" | 近 30 天 completedCount / totalCount 的平均值 |
| 最高完成率 | "最高完成率" | 单日最高 |
| 累计完成 | "累计完成" | 总 completedCount |
| 连续完成天数 | "连续全部完成" | 从今日往前计算连续完成率 100% 的天数 |

---

## 五、毛玻璃视觉规范

### 5.1 渐变背景（GradientBackground）

| 模式 | 顶部色值 | 底部色值 |
|------|----------|----------|
| 浅色 | #E8EAF6 (淡薰衣草) | #F3E5F5 (淡粉紫) |
| 深色 | #1B1B2F (深靛蓝) | #162447 (深海蓝) |

- 使用 `Brush.verticalGradient` 铺满全屏
- 所有页面共享同一背景组件

### 5.2 毛玻璃卡片（GlassCard）

| 参数 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 背景色 | Color.White.copy(alpha=0.25f) | Color.White.copy(alpha=0.08f) |
| 边框色 | Color.White.copy(alpha=0.4f) | Color.White.copy(alpha=0.15f) |
| 边框宽度 | 1dp | 1dp |
| 圆角 | 16dp | 16dp |
| 阴影 | elevation=8dp, alpha=0.1 | elevation=8dp, alpha=0.2 |
| 内边距 | 16dp | 16dp |

不使用实时高斯模糊（性能考虑），通过上述半透明叠层模拟毛玻璃效果。

### 5.3 毛玻璃底部 Sheet（GlassBottomSheet）

- 圆角：顶部左右 24dp
- 背景：浅色 `White alpha=0.85f`，深色 `Color(0xFF1E1E2E) alpha=0.92f`
- 拖拽手柄：顶部居中，宽 40dp 高 4dp 圆角横条
- 内边距：horizontal 20dp，top 12dp，bottom = navigationBarPadding + 20dp

### 5.4 毛玻璃对话框（GlassDialog）

- 宽度：屏幕 85%
- 圆角：20dp
- 背景色与卡片相同但 alpha 稍高（浅色 0.7，深色 0.3）
- 居中显示，外部半透明遮罩 `Black alpha=0.4`

### 5.5 底部导航栏

- 背景：浅色 `White alpha=0.6f`，深色 `White alpha=0.1f`
- 顶部边框线：`White alpha=0.3f` 1dp
- 高度：64dp
- 圆角：顶部左右 20dp
- 选中项：图标 + 文字颜色为主题强调色
- 未选中项：`White alpha=0.5f`（深色）/ `Gray alpha=0.6f`（浅色）

### 5.6 色彩系统

| 用途 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 主要文字 | #1C1B1F | #E6E1E5 |
| 次要文字 | #49454F | #CAC4D0 |
| 强调色/主题色 | #6750A4 (M3 Purple) | #D0BCFF |
| 完成状态色 | #4CAF50 | #81C784 |
| 删除/危险色 | #F44336 | #EF9A9A |
| 勾选框未选 | #79747E | #938F99 |
| 勾选框已选 | 同强调色 | 同强调色 |

### 5.7 字体排版

| 用途 | 大小 | 字重 |
|------|------|------|
| 页面标题 | 24sp | Bold |
| 卡片标题（如 "今日"）| 18sp | SemiBold |
| 待办内容文字 | 16sp | Normal |
| 次要信息（日期、统计）| 14sp | Normal |
| 底部导航标签 | 12sp | Medium |

---

## 六、完整技术依赖清单

```kotlin
// project build.gradle.kts plugins
plugins {
    id("com.android.application") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
    id("com.google.devtools.ksp") version "1.9.24-1.0.20" apply false
    id("com.google.dagger.hilt.android") version "2.51.1" apply false
}

// app build.gradle.kts
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.google.devtools.ksp")
    id("com.google.dagger.hilt.android")
}

android {
    namespace = "com.todo"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.todo"
        minSdk = 31
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }
}

dependencies {
    // Compose BOM
    val composeBom = platform("androidx.compose:compose-bom:2024.06.00")
    implementation(composeBom)
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.activity:activity-compose:1.9.0")
    debugImplementation("androidx.compose.ui:ui-tooling")

    // Navigation
    implementation("androidx.navigation:navigation-compose:2.7.7")

    // Room
    implementation("androidx.room:room-runtime:2.6.1")
    implementation("androidx.room:room-ktx:2.6.1")
    ksp("androidx.room:room-compiler:2.6.1")

    // DataStore
    implementation("androidx.datastore:datastore-preferences:1.1.1")

    // Hilt
    implementation("com.google.dagger:hilt-android:2.51.1")
    ksp("com.google.dagger:hilt-android-compiler:2.51.1")
    implementation("androidx.hilt:hilt-navigation-compose:1.2.0")
    implementation("androidx.hilt:hilt-work:1.2.0")
    ksp("androidx.hilt:hilt-compiler:1.2.0")

    // Lifecycle
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.8.2")
    implementation("androidx.lifecycle:lifecycle-runtime-compose:2.8.2")

    // WorkManager
    implementation("androidx.work:work-runtime-ktx:2.9.0")

    // Vico 图表
    implementation("com.patrykandpatrick.vico:compose-m3:2.0.0-beta.1")

    // 拖拽排序
    implementation("sh.calvin.reorderable:reorderable:1.5.0")

    // Coroutines
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")
}
```

---

## 七、DAO 接口完整定义

### 7.1 TodoDao

```kotlin
@Dao
interface TodoDao {
    @Query("SELECT * FROM todos WHERE date = :date ORDER BY sortOrder ASC")
    fun getTodosByDate(date: String): Flow<List<TodoEntity>>

    @Query("SELECT * FROM todos WHERE date = :date ORDER BY sortOrder ASC")
    suspend fun getTodosByDateSnapshot(date: String): List<TodoEntity>

    @Query("SELECT * FROM todos WHERE date IN (:dates) ORDER BY date DESC, sortOrder ASC")
    fun getTodosForDates(dates: List<String>): Flow<List<TodoEntity>>

    @Query("SELECT COALESCE(MAX(sortOrder), 0) + 1 FROM todos WHERE date = :date")
    suspend fun getNextSortOrder(date: String): Int

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(todo: TodoEntity): Long

    @Update
    suspend fun update(todo: TodoEntity)

    @Delete
    suspend fun delete(todo: TodoEntity)

    @Query("DELETE FROM todos WHERE date = :date")
    suspend fun deleteAllByDate(date: String)

    @Query("UPDATE todos SET isCompleted = :isCompleted WHERE id = :id")
    suspend fun updateCompleted(id: Long, isCompleted: Boolean)

    @Query("UPDATE todos SET sortOrder = :sortOrder WHERE id = :id")
    suspend fun updateSortOrder(id: Long, sortOrder: Int)

    @Query("UPDATE todos SET content = :content WHERE id = :id")
    suspend fun updateContent(id: Long, content: String)

    @Query("SELECT DISTINCT date FROM todos WHERE date < :today ORDER BY date DESC")
    suspend fun getHistoryDates(today: String): List<String>

    @Query("SELECT * FROM todos WHERE date < :cutoffDate")
    suspend fun getTodosBeforeDate(cutoffDate: String): List<TodoEntity>

    @Query("DELETE FROM todos WHERE date < :cutoffDate")
    suspend fun deleteTodosBeforeDate(cutoffDate: String)
}
```

### 7.2 PresetDao

```kotlin
@Dao
interface PresetDao {
    @Query("SELECT * FROM presets ORDER BY createdAt DESC")
    fun getAllPresets(): Flow<List<PresetEntity>>

    @Query("SELECT * FROM presets WHERE content LIKE '%' || :keyword || '%'")
    fun searchPresets(keyword: String): Flow<List<PresetEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(preset: PresetEntity): Long

    @Update
    suspend fun update(preset: PresetEntity)

    @Delete
    suspend fun delete(preset: PresetEntity)

    @Query("DELETE FROM presets WHERE id IN (:ids)")
    suspend fun deleteByIds(ids: List<Long>)
}
```

### 7.3 RecycleBinDao

```kotlin
@Dao
interface RecycleBinDao {
    @Query("SELECT * FROM recycle_bin ORDER BY deletedAt DESC")
    fun getAll(): Flow<List<RecycleBinEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(item: RecycleBinEntity)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertAll(items: List<RecycleBinEntity>)

    @Delete
    suspend fun delete(item: RecycleBinEntity)

    @Query("DELETE FROM recycle_bin WHERE id IN (:ids)")
    suspend fun deleteByIds(ids: List<Long>)

    @Query("DELETE FROM recycle_bin WHERE deletedAt < :cutoffTimestamp")
    suspend fun deleteOlderThan(cutoffTimestamp: Long)

    @Query("SELECT COUNT(*) FROM recycle_bin")
    fun getCount(): Flow<Int>
}
```

### 7.4 DailyStatsDao

```kotlin
@Dao
interface DailyStatsDao {
    @Query("SELECT * FROM daily_stats ORDER BY date DESC LIMIT :limit")
    fun getRecentStats(limit: Int = 30): Flow<List<DailyStatsEntity>>

    @Query("SELECT * FROM daily_stats WHERE date BETWEEN :startDate AND :endDate ORDER BY date ASC")
    fun getStatsBetween(startDate: String, endDate: String): Flow<List<DailyStatsEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(stats: DailyStatsEntity)

    @Query("SELECT * FROM daily_stats WHERE date = :date")
    suspend fun getByDate(date: String): DailyStatsEntity?
}
```

---

## 八、Entity 定义

```kotlin
@Entity(
    tableName = "todos",
    indices = [Index(value = ["date"])]
)
data class TodoEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val content: String,
    val isCompleted: Boolean = false,
    val date: String,       // "yyyy-MM-dd"
    val sortOrder: Int,
    val createdAt: Long = System.currentTimeMillis()
)

@Entity(tableName = "presets")
data class PresetEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val content: String,
    val createdAt: Long = System.currentTimeMillis()
)

@Entity(
    tableName = "recycle_bin",
    indices = [Index(value = ["deletedAt"])]
)
data class RecycleBinEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val originalDate: String,
    val content: String,
    val wasCompleted: Boolean,
    val deletedAt: Long = System.currentTimeMillis()
)

@Entity(tableName = "daily_stats")
data class DailyStatsEntity(
    @PrimaryKey val date: String,   // "yyyy-MM-dd"
    val totalCount: Int,
    val completedCount: Int
)
```

---

## 九、核心业务逻辑

### 9.1 每日归档逻辑（ArchiveUseCase + DailyArchiveWorker）

**触发时机：**
1. App 启动时，检查上次归档日期（DataStore 存储 `last_archive_date`），如果不是今天则执行归档
2. WorkManager 每日凌晨 00:05 执行（作为保险机制）

**归档流程：**
```
1. 获取所有 date < 今天 且尚未写入 DailyStats 的日期
2. 对每个日期：
   a. 查询该日所有 TodoEntity
   b. 计算 totalCount 和 completedCount
   c. 写入/更新 DailyStatsEntity
3. 获取所有 date < (今天 - 7天) 的 TodoEntity
4. 将这些转为 RecycleBinEntity 批量插入回收站
5. 删除这些原始 TodoEntity
6. 删除回收站中 deletedAt < (今天 - 30天) 的记录
7. 更新 DataStore 中 last_archive_date = 今天
```

### 9.2 实时统计更新

今日待办每次 toggle 完成状态时，同步更新 DailyStatsEntity（date=今天）：
```kotlin
suspend fun refreshTodayStats() {
    val today = DateUtils.today()
    val todos = todoDao.getTodosByDateSnapshot(today)
    val stats = DailyStatsEntity(
        date = today,
        totalCount = todos.size,
        completedCount = todos.count { it.isCompleted }
    )
    dailyStatsDao.insert(stats) // REPLACE 策略
}
```

### 9.3 拖拽排序保存

```kotlin
suspend fun reorder(reorderedList: List<Todo>) {
    reorderedList.forEachIndexed { index, todo ->
        todoDao.updateSortOrder(todo.id, index)
    }
}
```

---

## 十、WorkManager 配置

```kotlin
@HiltWorker
class DailyArchiveWorker @AssistedInject constructor(
    @Assisted context: Context,
    @Assisted params: WorkerParameters,
    private val archiveUseCase: ArchiveUseCase,
    private val cleanupUseCase: CleanupUseCase
) : CoroutineWorker(context, params) {

    override suspend fun doWork(): Result {
        return try {
            archiveUseCase()
            cleanupUseCase()
            Result.success()
        } catch (e: Exception) {
            Result.retry()
        }
    }
}

// 在 TodoApp 初始化时注册：
fun scheduleDailyArchive(context: Context) {
    val constraints = Constraints.Builder()
        .setRequiresBatteryNotLow(false)
        .build()

    val dailyRequest = PeriodicWorkRequestBuilder<DailyArchiveWorker>(
        1, TimeUnit.DAYS
    )
        .setInitialDelay(calculateDelayUntilMidnight(), TimeUnit.MILLISECONDS)
        .setConstraints(constraints)
        .build()

    WorkManager.getInstance(context).enqueueUniquePeriodicWork(
        "daily_archive",
        ExistingPeriodicWorkPolicy.KEEP,
        dailyRequest
    )
}
```

---

## 十一、ViewModel 状态设计

### 11.1 TodoViewModel

```kotlin
@HiltViewModel
class TodoViewModel @Inject constructor(...) : ViewModel() {

    data class UiState(
        val todayTodos: List<Todo> = emptyList(),
        val historyRecords: List<DailyRecord> = emptyList(),
        val todayStats: DailyStats = DailyStats("", 0, 0),
        val isLoading: Boolean = true
    )

    private val _uiState = MutableStateFlow(UiState())
    val uiState: StateFlow<UiState> = _uiState.asStateFlow()

    // 事件：添加、删除、编辑、排序、勾选
}
```

### 11.2 PresetViewModel

```kotlin
data class PresetUiState(
    val presets: List<Preset> = emptyList(),
    val isMultiSelectMode: Boolean = false,
    val selectedIds: Set<Long> = emptySet(),
    val searchQuery: String = ""
)
```

### 11.3 ChartViewModel

```kotlin
data class ChartUiState(
    val stats: List<DailyStats> = emptyList(),
    val chartType: ChartType = ChartType.BAR,  // BAR or LINE
    val averageRate: Float = 0f,
    val maxRate: Float = 0f,
    val totalCompleted: Int = 0,
    val consecutiveDays: Int = 0
)
```

### 11.4 RecycleBinViewModel

```kotlin
data class RecycleBinUiState(
    val items: List<RecycleBinItem> = emptyList(),
    val isMultiSelectMode: Boolean = false,
    val selectedIds: Set<Long> = emptySet()
)
```

---

## 十二、动画与微交互规范

| 交互 | 动画效果 |
|------|----------|
| 待办勾选 | 勾选框 scale 弹性动画 0→1，内容文字 alpha 1→0.5 + 删除线从左到右划出 (300ms) |
| 添加待办 | 新条目从底部 slideIn + fadeIn (250ms) |
| 删除待办 | 条目向左 slideOut + fadeOut (200ms) + 下方条目上移 (200ms) |
| 拖拽中 | 被拖项 scale=1.05 + elevation=16dp，其他项自动让位动画 |
| 底部 Sheet 弹出 | 从下方 slideUp + fadeIn (300ms, EaseOutCubic) |
| 页面切换 | 底部 Tab 切换使用 crossfade (200ms) |
| FAB 按下 | scale 0.9→1.0 弹性回弹 |
| 多选勾选 | 圆形勾选框 scale 弹性 + 颜色渐变 (200ms) |

---

## 十三、性能优化要点

| 优化项 | 措施 |
|--------|------|
| LazyColumn | 使用 `key = { it.id }` 避免不必要重组 |
| Room Flow | 使用 `distinctUntilChanged()` 减少重复通知 |
| 图片/渐变 | 背景 Brush 使用 `remember` 缓存 |
| 拖拽排序 | 松手后批量 update，非拖拽中途实时写库 |
| Compose 重组 | 使用 `@Stable` / `@Immutable` 标注数据类 |
| 启动速度 | Hilt 编译期注入，避免反射 |
| 数据库 | 为 `todos.date` 和 `recycle_bin.deletedAt` 添加索引（已在 Entity 定义中声明） |
| WorkManager | PeriodicWork 使用 KEEP 策略避免重复注册 |

---

## 十四、简体中文界面文字汇总

> 以下为项目中所有用户可见的界面文字，Codex 实现时必须原样使用，**不得使用英文替代**。

| 位置 | 文字 |
|------|------|
| 底部导航 Tab | "预设"、"待办"、"设置" |
| 待办页 - 迷你统计 | "{N}/{M} 已完成" |
| 待办页 - 今日标题 | "今日" |
| 待办页 - 日期格式 | "2025年1月15日 周三" |
| 待办页 - 空状态 | "点击右下角 + 添加第一条待办" |
| 往日卡片 - 完成率 | "{N}/{M} 完成" |
| 添加 Sheet - Tab 标题 | "手动输入"、"从预设选择" |
| 添加 Sheet - placeholder | "输入待办内容..." |
| 添加 Sheet - 按钮 | "添加"、"完成" |
| 添加 Sheet - 搜索框 | "搜索预设..." |
| 添加 Sheet - 空预设 | "暂无预设，去预设页面添加吧" |
| 添加 Sheet - 多选按钮 | "添加选中项" |
| 编辑 Sheet - 标题 | "编辑待办" |
| 编辑 Sheet - 按钮 | "保存"、"删除" |
| 删除确认对话框 | "确定删除这条待办吗？" |
| 删除确认按钮 | "确定"、"取消" |
| 撤销提示 | "已删除" + "撤销" 按钮 |
| 预设页 - 顶栏标题 | "我的预设" |
| 预设页 - 多选顶栏 | "已选 {N} 项" |
| 预设页 - 按钮 | "全选"、"删除" |
| 预设页 - 空状态 | "点击右上角 + 创建常用待办预设" |
| 预设页 - 删除确认 | "确定删除选中的 {N} 条预设吗？" |
| 预设编辑对话框 - 新建标题 | "新建预设" |
| 预设编辑对话框 - 编辑标题 | "编辑预设" |
| 预设编辑对话框 - placeholder | "输入预设内容..." |
| 预设编辑对话框 - 按钮 | "取消"、"创建"、"保存" |
| 设置页 - 顶栏标题 | "设置" |
| 设置页 - 外观主题 | "外观主题" |
| 设置页 - 主题选项 | "跟随系统"、"浅色模式"、"深色模式" |
| 设置页 - 回收站入口 | "回收站" |
| 设置页 - 关于 | "关于" |
| 设置页 - 版本信息 | "Todo v1.0" |
| 回收站 - 顶栏标题 | "回收站" |
| 回收站 - 多选顶栏 | "已选 {N} 项" |
| 回收站 - 按钮 | "全选"、"永久删除" |
| 回收站 - 删除确认 | "确定永久删除 {N} 条记录？此操作不可撤销" |
| 回收站 - 空状态 | "回收站为空" |
| 图表页 - 顶栏标题 | "完成统计" |
| 图表页 - 图表类型 | "柱状图"、"折线图" |
| 图表页 - 统计标签 | "平均完成率"、"最高完成率"、"累计完成"、"连续全部完成" |
| 图表页 - 连续天数单位 | "{N} 天" |
| 通用 - 星期 | "周一"、"周二"、"周三"、"周四"、"周五"、"周六"、"周日" |
| 通用 - 确认/取消 | "确定"、"取消" |

---

## 十五、DateUtils 日期工具规范

```kotlin
object DateUtils {
    // 返回今天的日期字符串，格式 "yyyy-MM-dd"
    fun today(): String

    // 返回指定天数之前的日期字符串，格式 "yyyy-MM-dd"
    fun daysAgo(days: Int): String

    // 将 "yyyy-MM-dd" 格式化为中文显示，如 "2025年1月15日 周三"
    fun formatDisplayDate(dateStr: String): String

    // 将 "yyyy-MM-dd" 格式化为中文短日期，如 "2025年1月15日"
    fun formatShortDate(dateStr: String): String

    // 获取最近 N 天的日期列表（不含今天），从近到远排列
    fun getRecentDates(days: Int): List<String>

    // 计算距离下一个午夜的毫秒数（用于 WorkManager 初始延迟）
    fun calculateDelayUntilMidnight(): Long

    // 获取星期几的中文名称
    fun getChineseDayOfWeek(dateStr: String): String
}
```

**星期中文映射：**
- MONDAY → "周一"
- TUESDAY → "周二"
- WEDNESDAY → "周三"
- THURSDAY → "周四"
- FRIDAY → "周五"
- SATURDAY → "周六"
- SUNDAY → "周日"

---

## 十六、导航路由定义

```kotlin
sealed class Screen(val route: String) {
    // 底部 Tab 页面
    object Todo : Screen("todo")
    object Preset : Screen("preset")
    object Settings : Screen("settings")

    // Push 页面（非 Tab）
    object RecycleBin : Screen("recycle_bin")
    object Chart : Screen("chart")
}
```

**导航规则：**
- 底部 3 个 Tab 之间使用 `navigate` + `launchSingleTop = true` + `popUpTo(startDestination) { saveState = true }` + `restoreState = true`
- RecycleBin 和 Chart 页面使用普通 `navigate` push 进入，顶部带返回箭头，点击返回 `popBackStack()`
- 默认启动页为 `Screen.Todo`

---

## 十七、Hilt 模块定义

### 17.1 DatabaseModule

```kotlin
@Module
@InstallIn(SingletonComponent::class)
object DatabaseModule {

    @Provides
    @Singleton
    fun provideDatabase(@ApplicationContext context: Context): AppDatabase {
        return Room.databaseBuilder(
            context,
            AppDatabase::class.java,
            "todo_database"
        ).build()
    }

    @Provides
    fun provideTodoDao(db: AppDatabase): TodoDao = db.todoDao()

    @Provides
    fun providePresetDao(db: AppDatabase): PresetDao = db.presetDao()

    @Provides
    fun provideRecycleBinDao(db: AppDatabase): RecycleBinDao = db.recycleBinDao()

    @Provides
    fun provideDailyStatsDao(db: AppDatabase): DailyStatsDao = db.dailyStatsDao()
}
```

### 17.2 AppDatabase

```kotlin
@Database(
    entities = [
        TodoEntity::class,
        PresetEntity::class,
        RecycleBinEntity::class,
        DailyStatsEntity::class
    ],
    version = 1,
    exportSchema = false
)
abstract class AppDatabase : RoomDatabase() {
    abstract fun todoDao(): TodoDao
    abstract fun presetDao(): PresetDao
    abstract fun recycleBinDao(): RecycleBinDao
    abstract fun dailyStatsDao(): DailyStatsDao
}
```

---

## 十八、AndroidManifest 关键配置

```xml
<application
    android:name=".TodoApp"
    android:label="Todo"
    android:theme="@style/Theme.Todo"
    ...>

    <activity
        android:name=".MainActivity"
        android:screenOrientation="portrait"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- HiltWorkerFactory 由 Hilt 自动注册 -->
    <provider
        android:name="androidx.startup:InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        tools:node="remove" />
</application>
```

---

## 十九、构建与运行检查清单

Codex 构建完成后，请确认以下所有条件均满足：

- [ ] 项目根目录为 `D:\Code\2.Projects\Todo`
- [ ] 包名为 `com.todo`
- [ ] `app_name` 为 "Todo"
- [ ] 所有用户可见的界面文字均为简体中文（参照第十四节汇总表）
- [ ] 日期显示格式为中文（如 "2025年1月15日 周三"）
- [ ] 底部导航栏 3 个 Tab 标签为 "预设"、"待办"、"设置"
- [ ] 默认启动页为"待办"页
- [ ] 浅色/深色主题可正常切换，默认跟随系统
- [ ] Room 数据库包含 4 张表，索引已添加
- [ ] WorkManager 每日归档任务已注册
- [ ] 拖拽排序功能正常工作
- [ ] 回收站从设置页 push 进入，图表页从迷你统计卡 push 进入
- [ ] 所有毛玻璃组件样式符合第五节视觉规范
- [ ] 动画效果符合第十二节规范
- [ ] 编译无错误，可直接运行

---

*文档结束。Codex 请严格按照以上全部内容构建项目，不得遗漏任何细节。*
